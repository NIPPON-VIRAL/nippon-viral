{
  "name": "nippon-viral",
  "version": "1.0.0",
  "description": "日本のSNSバズり旅 — 毎日更新トレンドランキング",
  "type": "module",
  "scripts": {
    "dev": "npx serve public -p 3000",
    "generate": "node scripts/generate-ranking.js",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
